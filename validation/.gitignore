# Credentials must never live in the checkout; paths configured via
# environment variables are rejected if they resolve inside the repo.
.env
.env.*
*credentials*.json
*service-account*.json
*.key
*.candidate
.envrc
service_account.json
